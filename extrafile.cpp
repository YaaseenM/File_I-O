#include "extrafile.h"

#include <string>
using std::string;

/*class extrafile {
    int number;
    string Theme;
    string name;
    int minifigs;
    int pieces;
    int price;
public:
    extrafile(int number, string theme, string name, int minifigs, int pieces, int price);
    int GetNumber();
    string GetTheme();
    string GetName();
    int GetMinifigs();
    int GetPieces();
    int GetPrice();
};
*/

int GetNumber() {
    return number;
}